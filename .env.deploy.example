# docker compose 变量文件模板 —— 复制为 .env（放在仓库根目录，与 docker-compose.yml 同级）。
# 这些是"构建/穿透"用变量；后端运行时机密另放 backend/.env。

# 前端高德 JS API key（构建期打进前端 bundle；需在高德控制台把 coderlx.dpdns.org 加入安全域名）
VITE_AMAP_JS_KEY=填你的-高德JS-key
VITE_AMAP_JS_SECURITY_CODE=填你的-高德JS-安全密钥

# Cloudflare Tunnel token（面板创建 Tunnel 后获得，见 DEPLOY.md 第 4 步）
TUNNEL_TOKEN=填你的-cloudflare-tunnel-token
